<div class="title">Listing Requests</div>

<table id="hor-zebra" summary="Request">
  <thead>
    	<tr>
            <th scope="col">Reason</th>
            <th scope="col">Amount</th>
            <th scope="col">Request date</th>
	    <th scope="col">Solicitante</th>
            <th scope="col">Comite del Solicitante</th>
	    <th scope="col">Area Funcional</th>
            <th scope="col">Para</th>
            <th scope="col">Status</th>
    	    <th scope="col"></th>
	    <% if session[:user].role=='Admin' %>
    	    <th scope="col"></th>
    	    <th scope="col"></th>
	    <% end %>
        </tr>
  </thead>
    

<% @requests.each do |request| %>
<tr class="odd">
    <td><%= request.reason %></td>
    <td><%= request.amount %></td>
    <td><%= request.request_date %></td>
    <td><%= request.user.username %></td>
    <td><%= request.user.committee %></td>
    <td><%= request.user.area %></td>
    <td><%= request.para %></td>
    <td><%= request.status %></td>
    <td><%= link_to 'Show', request %></td>
    <td><%= link_to 'Edit', edit_request_path(request) %></td>
    <td><%= link_to 'Destroy', request, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to 'New Request', new_request_path %> |
<%= link_to 'Back', "/home/index" %>
